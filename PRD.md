# 生活规划 App 产品需求文档（PRD · 第一版）

> 版本 v1.0 ｜ 定位：个人学习与娱乐用的专属生活规划 App
> 运行方式：仅本机浏览器本地运行，纯本地存储，无登录、无联网、无云数据库、无部署、无手机端。

## 1. 产品目标和使用场景

**目标**
- 一个只给使用者自己用的本地生活规划 App，把学习、锻炼、饮食、娱乐、课表、音乐、备忘统一管理。
- 打开 App 一眼看到「今天该做什么」；每个模块有各自专属的用法。
- 数据保存在电脑本地，关机、重启、刷新页面都不丢失。

**使用场景**
- 学生（或按学期节奏生活的人）日常规划：每天看首页掌握当天学习任务、今日课程和待办。
- 按周安排锻炼、按日期安排娱乐活动。
- 每天记录饮食、统计热量。
- 播放本地音乐时，切到 App 里点开音乐模块即可播放。
- 随手记待办，到期前在页面内看到提醒。

## 2. 整体页面框架和导航结构

- 单页应用，左侧固定导航栏，9 个入口（图标 + 文字）：
  1. 首页预览
  2. 学习计划
  3. 锻炼计划
  4. 饮食计划
  5. 娱乐计划
  6. 学期课表
  7. 音乐播放
  8. 备忘录
  9. 数据与设置
- 顶栏：当前页面标题；右侧常驻「今天日期」+「第 X 周 · 单/双周」（由学期课表配置算出）。
- 内容区：切换导航渲染对应模块；当前导航项高亮。
- 全局交互约定：
  - 增删改即时自动保存，无手动保存按钮；
  - 新增统一「+ 添加」；编辑/删除在列表项上出现；
  - 删除课程/任务/备忘等重要数据弹确认；表单用弹窗/抽屉完成，不跳页。

## 3. 首页的内容结构

- 顶部：问候语 + 今日日期 + 当前周次。
- 主卡片（三张，必显示）：
  1. 今天的学习任务（到期/逾期任务、进度、可勾选完成）
  2. 今日课表（按时间列出今天的课，含节次、地点、单双周标记）
  3. 未完成备忘（未完成待办，可直接打勾）
- 副卡片（今日摘要）：今天锻炼安排、今天娱乐活动、今日饮食热量小结。
- 卡片均可点击跳转到对应模块；首页卡片是否显示可在设置中开关。

## 4. 每个模块的用途

1. 首页预览 —— 汇总展示今天的关键信息（见第 3 节）。
2. 学习计划 —— 任务 + 进度追踪：按学科拆任务，带截止日期、目标和进度。
3. 锻炼计划 —— 每周锻炼日程表：按星期固定排锻炼安排，当天照做。
4. 饮食计划 —— 饮食记录 + 热量统计：记录每天实际吃了什么，统计热量/营养。
5. 娱乐计划 —— 娱乐活动安排：像日历一样安排周末/空闲时间的娱乐活动。
6. 学期课表 —— 每周课表，支持单双周；填开学日期自动算当前第几周。
7. 音乐播放 —— 从本地文件夹直接读取歌曲，记住授权，不复制文件，带基本播放。
8. 备忘录 —— 待办清单（可勾选）。
9. 数据与设置 —— 导出/导入备份、学期与节次设置、音乐授权、外观、首页卡片开关、关于。

## 5. 每个模块第一版最必须的数据和操作

### 5.1 学习计划
- 数据：学科（名称、颜色）；学习任务（标题、所属学科、截止日期、备注、子任务[{文本,完成}]、状态、创建时间、完成时间）。
- 操作：增删改学科；增删改任务；勾选子任务/完成；按「全部/今天到期/已逾期/已完成」筛选；进度 = 子任务勾选比例；逾期高亮。

### 5.2 锻炼计划
- 数据：锻炼条目（星期 1-7、名称、说明/动作组数时长、时间段）。
- 操作：按周一到周日 7 列展示；某天增删改条目、拖动排序；今天列可打「今日完成」勾（仅当天有效）。

### 5.3 饮食计划
- 数据：饮食记录（日期、四餐段：早餐/午餐/晚餐/加餐，每餐食物列表[名称、份量、热量、可选蛋白质/碳水/脂肪]）；常用食物预设。
- 操作：日期前后切换；某餐段添加食物（常用食物一键带入或手填）；存为常用；编辑/删除；当日热量小结 + 7 天趋势。

### 5.4 娱乐计划
- 数据：娱乐活动（标题、日期、开始/结束时间、地点、类型、备注、完成标记）。
- 操作：月历选日期查看/新增当天活动；编辑/删除/标记完成；有活动的日期在日历打点；「即将到来」列表。

### 5.5 学期课表
- 数据：学期设置（名称、开学日期=周一）；课程（名称、星期、节次、地点、每周/单周/双周、颜色）；节次时间表。
- 操作：设置学期后自动算周次/单双周；周次切换（本周/上一周/下一周/任意周）；点空格新建课程、点课程编辑/删除；单双周课程只在对应周显示；今天列高亮。
- 默认值（可在设置中改）：周一~周五上课，每天 10 节；默认节次时间：1-2 节 8:00-9:40、3-4 节 10:00-11:40、5-6 节 14:00-15:40、7-8 节 16:00-17:40、9-10 节 19:00-20:40。

### 5.6 音乐播放
- 数据：文件夹授权句柄、歌曲列表（文件名）、播放状态（当前曲目、进度、音量、循环/随机）。
- 操作：选择音乐文件夹→扫描音频文件生成列表；播放/暂停/上一首/下一首/进度拖动/音量/循环/随机；点击列表播放；记住上次播放位置；授权失效时「重新授权」。

### 5.7 备忘录
- 数据：待办（文本、完成标记、置顶、可选截止日期、顺序、创建时间）。
- 操作：顶部快速添加（回车）；勾选完成并划掉；编辑/删除/置顶；未完成与已完成分组；带截止日期的高亮。

### 5.8 数据与设置
- 数据：全局设置（学期、节次、外观、首页卡片开关、音乐授权状态）。
- 操作：导出全部数据（生成 .json 下载）；导入数据（校验后覆盖恢复，导入前自动备份）；学期/节次/外观/首页卡片配置；音乐重新授权/取消授权；关于版本号。

## 6. 首页和各模块之间的联系

- 学期课表的周次/单双周 → 供首页与顶栏全局展示。
- 学习任务、锻炼、娱乐、饮食、备忘的「今天」数据 → 汇总到首页各卡片。
- 首页每张卡片 → 点击跳转到对应模块并定位到今天/选中日期。
- 备份/恢复 → 一次覆盖全部模块数据，恢复后首页与各模块同步刷新。

## 7. 本地数据文件、备份和恢复要求

- 数据存浏览器本地数据库（IndexedDB）；音乐文件夹用浏览器文件系统授权句柄，不复制音频文件。
- 数据文件格式：单个 `.json` 文件，包含所有实体（设置、学科、学习任务、锻炼条目、饮食记录、常用食物、娱乐活动、课程、备忘等）与版本号。
- 备份：设置页「导出全部数据」→ 下载一个 JSON 文件到电脑。
- 恢复：选择文件 → 校验结构与必填字段 → 弹「将覆盖当前数据」确认 → 覆盖恢复；导入前自动先导出当前数据一份作为安全备份。
- 失败处理：文件损坏/结构不合法 → 报错且不覆盖现有数据；本地数据读取异常 → 提示可从备份导入，避免白屏。

## 8. 第一版必须要做的内容

- 9 个模块全部可用，且各自按第 4、5 节场景实现。
- 单页应用 + 左侧导航，顶栏显示日期和周次。
- 首页三主卡片 + 今日摘要副卡片。
- 全部数据本地存储，刷新/重启/关机不丢。
- 音乐从本地文件夹直读播放、记住授权。
- 数据导出/导入备份与恢复。
- 页面内提醒（今天/逾期高亮），无系统通知。

## 9. 第一版不做的内容

- 不做登录/账号、用户系统。
- 不做联网、云数据库、后端服务、部署、多人协作。
- 不做手机端/移动适配（桌面优先）。
- 不做系统级弹窗通知/后台提醒（仅页面内提醒）。
- 不复制/上传音乐文件、不做在线音乐源。
- 不做一键清空/重置数据（只做导出/导入备份，如需要后续再加）。
- 不做跨设备同步。
